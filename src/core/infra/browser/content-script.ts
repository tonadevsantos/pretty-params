// import { createMessenger } from "@/url/repositories/messages";
// import { browser } from "@wxt-dev/browser";

// const messenger = createMessenger(browser.runtime.sendMessage);

// // messenger.sendUrl(window.location.href);
